server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SanghoonSon/group-buying
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: admin
#    password: admin
#

# actuator 기본 설정
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true

encrypt:
  key: asdkfjaksdfjksd12341232